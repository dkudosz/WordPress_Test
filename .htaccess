
# BEGIN WordPress
# The directives (lines) between "BEGIN WordPress" and "END WordPress" are
# dynamically generated, and should only be modified via WordPress filters.
# Any changes to the directives between these markers will be overwritten.
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /wp-tester/
RewriteRule ^index\.php$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /wp-tester/index.php [L]
</IfModule>

# END WordPress
# BEGIN MiddleWare Authentication Rewrite
# The directives (lines) between "BEGIN MiddleWare Authentication Rewrite" and "END MiddleWare Authentication Rewrite" are
# dynamically generated, and should only be modified via WordPress filters.
# Any changes to the directives between these markers will be overwritten.

<IfModule mod_rewrite.c>
	RewriteEngine On
	RewriteRule ^(.*/)?files/(.*).pdf index.php?file_path=%{REQUEST_URI} [L]
	RewriteRule ^(.*/)?uploads/(.*).pdf index.php?file_path=%{REQUEST_URI} [L]
</IfModule>
				
# END MiddleWare Authentication Rewrite